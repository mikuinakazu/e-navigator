= render 'layouts/header'
.interview_wrapper
  %h1 ユーザー一覧☆
  %table.table
    %thead
      %tr
        %th 名前
        %th Email
        %th 年齢
        %th 性別
        %th 学校名
        %th 面接日時
        %th
    %tbody
    - @users.each do |user|
      - if user.profile.exists?
        %tr
          %td
            = user.profile.name
          %td
            = user.email
          %td
            = user.profile.birth_date
          %td
            = user.profile.sex
          %td
            = user.profile.school
          %td
            = user.interviews.last.interview_date if user.interviews.exists?
          %td
            =link_to '面接一覧'

