%p これは、インタビュアー用の部分テンプレートです！
%h1 #{@user.profile.name}さんの面接一覧
%h2 現在の面接日程
- if @user.interviews.present?
  %b
    #{@user.interviews.last.interview_date}
  に面接が設定されています。
- else
  面接日程はまだ設定されていません。
%hr
%h2 面接日程を変更する場合は以下から選んでください。
/ 今日から10日後、時間はランダムに10個表示
.btn
  = @today
  = @time
- 10.times do
  .btn
    = @random_days
    = @random_time

