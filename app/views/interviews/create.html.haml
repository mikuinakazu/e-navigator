= render 'layouts/header'
.interview_wrapper
  .alert.alert_info 面接が作成されました
  %p.strong 開始時間：
  %p.strong.inline-block 承認状態：
  %p.inline-block 保留
  .block
    =link_to '自分の面接一覧へ', user_interviews_path, class: 'btn'

